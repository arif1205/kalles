import styled from "styled-components";

export const Container = styled.div`
	background: #f6f6f8;
	color: var(--gray-color);
	font-size: 12px;
	font-weight: 300;
	padding: 11px 15px;

	.icon {
		margin-right: 5px;
	}

	.icon-right {
		margin-left: 5px;
	}

	.discount_amount {
		color: var(--red-color);
	}
`;
